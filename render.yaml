services:
  - type: web
    name: catholic-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: DATABASE_URL
        value: postgresql://postgres.gpcachimljhpjgcvoasf:CatholicReadings2026!@aws-0-eu-west-1.pooler.supabase.com:6543/postgres